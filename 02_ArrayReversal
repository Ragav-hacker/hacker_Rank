#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num, *arr, i;
    scanf("%d", &num);
    arr = (int*) malloc(num * sizeof(int));
    for(i = 0; i < num; i++) {
        scanf("%d", arr + i);
    }
int *rev=(int *) malloc (num * sizeof(int));

for(int i=num-1,j=0; i>=0; i--,j++){
    rev[j]=arr[i];
    
}

  

    for(i = 0; i < num; i++)
        printf("%d ", *(rev + i));
    return 0;
    free(arr);
    free(rev);
}
